#!/usr/bin/python

import sys
"""
previous = None


for line in sys.stdin:
    key, value = line.split('\t')
    
    if key != previous:
        if previous is not None:
            print(previous + '\t' + value)
        previous = key

print(previous + '\t' + value)
"""